---
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/port: "5556"
    prometheus.io/scrape: "true"
  name: kafka-broker
  namespace: staging
spec:
  ports:
  - port: 9092
  selector:
    app: kafka
